import groovy.json.JsonSlurper

@Library('common-lib') _

pipeline {
    agent any

    environment {
        TEST = 'TEST'
    }

    stages {
        stage('test') {
            steps {
                println("github-file-test")
                callTest()
            }
        }
    }
}
