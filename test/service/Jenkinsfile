import groovy.json.JsonSlurper

pipeline {
    agent any

    environment {
    }

    stages {

        stage('test') {
            steps {
                println("github-file-test")
            }
        }
    }
}
