import groovy.json.JsonSlurper

pipeline {
    agent any

    environment {
        TEST = 'TEST'
    }

    stages {

        stage('test') {
            steps {
                println("github-file-test")
            }
        }
    }
}
